#include<stdio.h>

int main(){
    
    int list[5];
    int i;
    int sum;
    sum=0;
      for(i=0;i<5;++i)
  {    
    printf("Enter Num: ");
    scanf("%d",&list[i]);
    sum=sum+list[i];
  }
    printf("Sum is:%d",sum);
        return 0;
        } 
